Доброго времени суток проверяющему!

users.py написан самостоятельно со вставками кода из модуля.
find_athlete.py скопирован под чистую, т.к. не придумал как это сделать, хотя пытался(
delete table user.py просто удаляет пользователей из таблицы user.

Код user.py написан не тем способом, который описывается в модуле, но работает. 
Мой способ (https://pythonru.com/osnovy/sqlite-v-python).

Спасибо большое за проверку.
	
